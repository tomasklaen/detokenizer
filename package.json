{
	"name": "detokenizer",
	"version": "1.0.1",
	"description": "Replace tokens in a string.",
	"author": "tomasklaen",
	"repository": "https://github.com/tomasklaen/detokeniser",
	"license": "MIT",
	"keywords": [
		"string",
		"replace",
		"token"
	],
	"files": [
		"dist"
	],
	"main": "./dist/main.js",
	"types": "./dist/main.d.ts",
	"scripts": {
		"test": "jest",
		"clean": "rm -rf dist",
		"build": "npm run clean & tsc -p tsconfig.build.json",
		"dev": "npm run clean & jest --watchAll"
	},
	"devDependencies": {
		"@types/jest": "^26.0.21",
		"@types/node": "^15.0.1",
		"jest": "^26.6.3",
		"ts-jest": "^26.5.4",
		"typescript": "^4.2.3"
	}
}
